#include <stdio.h>
#include <stdbool.h>

int main() {

    bool verdadeiro = true;

    printf("Verdadeiro: %d\n", verdadeiro);
    printf("Verdadeiro negado: %d\n", !verdadeiro);

    bool falso = false;

    printf("Falso: %d\n", falso);
    printf("Falso negado: %d\n", !falso);

}